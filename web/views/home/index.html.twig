Explain what Seedstream is...

{% if is_granted('ROLE_USER') %}
    <p>You are already logged in</p>
    <a href="{{ path('dashboard') }}">Dashboard</a>
    <a href="{{ path('logout') }}">Logout</a>
{% else %}
    <p>You are not currently logged in</p>
    <a href="{{ path('login') }}">Login</a>
{% endif %}